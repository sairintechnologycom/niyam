# niyam guard freeze

Restrict AI edits to a specific directory.

## Usage

```bash
niyam guard freeze [OPTIONS] PATH
```

## Arguments

* `path`: Path to restrict edits to.

## Options

* `--dry-run`: Preview changes without writing. (Default: False)

