# sutra runtime add

Add and configure a new AI runtime.

## Usage

```bash
sutra runtime add [OPTIONS] RUNTIME
```

## Arguments

* `runtime`: Runtime to add.

## Options

* `--dry-run`: Preview changes without writing. (Default: False)

