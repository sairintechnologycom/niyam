# sutra pack add

Install a pack into the workspace.

## Usage

```bash
sutra pack add [OPTIONS] NAME
```

## Arguments

* `name`: Name of the pack to add.

## Options

* `--force`: Overwrite existing files. (Default: False)

