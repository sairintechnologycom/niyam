# sutra sync

Sync .sutra/ source of truth to all configured runtimes.

## Usage

```bash
sutra sync [OPTIONS]
```

## Options

* `--runtime, -r`: Sync a specific runtime only.
* `--verbose, -v`: Show detailed file updates during synchronization. (Default: False)
* `--dry-run`: Preview sync changes without writing files. (Default: False)

