# sutra memory add

Append a note to a memory file.

## Usage

```bash
sutra memory add FILE NOTE
```

## Arguments

* `file`: Memory file to append to (e.g. project-lessons).
* `note`: Note to append.

