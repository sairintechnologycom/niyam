# Command policy — commands that are denied or warned
deny:
  - rm -rf
  - sudo
  - chmod 777
  - git push --force
  - git reset --hard
  - npm publish
  - terraform apply
  - terraform destroy
  - "curl * | sh"
  - DROP TABLE
  - DROP DATABASE

warn: []
