# niyam ci verify

Verify cryptographic integrity, guardrails, and validation status for CI/CD.

## Usage

```bash
niyam ci verify [OPTIONS]
```

## Options

* `--target, -t`: Target branch to compare changes against. (Default: main)
* `--strict`: Fail build on integrity warnings or missing evidence. (Default: True)

