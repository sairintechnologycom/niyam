# niyam dashboard

Show real-time dashboard of the active or latest mission.

## Usage

```bash
niyam dashboard [OPTIONS]
```

## Options

* `--watch, -w`: Periodically refresh the dashboard (live mode). (Default: False)

